manifest:
  remotes:
    - name: infused-kim-forks
      url-base: https://github.com/jannis-baum

  projects:

    - name: zmk
      remote: infused-kim-forks
      # Pinned to a commit for reproducible builds. This is the HEAD of my fork's
      # `pr-testing/mouse_ps2_module_base` branch. To update, move the branch (or
      # re-sync the fork) and replace this SHA with the new HEAD.
      revision: dbbeb701aee215fbd5f5e9faecc778b745d4cf87
      import: app/west.yml

    # Module for PS/2 mouse & TP driver
    - name: kb_zmk_ps2_mouse_trackpoint_driver
      remote: infused-kim-forks
      # Pinned commit = HEAD of my fork's `main` branch.
      revision: f6f3b30677caeccc4970bb48f48240d3504d03f7

  self:
    path: firmware
